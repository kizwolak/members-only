extends layout 

block content 

    h2 #{title}
    h3 Club code: #{code}
    .float-right
        h3 Members: 
        ul 
            for member in members
                li #{member.full_name} (#{member.username}) 